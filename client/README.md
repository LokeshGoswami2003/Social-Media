// deploying project
aws
account -> ec2 Instance -> install nvm node pm2 ->
/home/ubuntu/
└── apps/
├── social-media/
│ ├── server/ (Node backend)
│ ├── client/ (Frontend)
│ └── README.md
│
├── app2/ (future)
└── app3/ (future) // file dirctory

then will clone git clone social media inside apps ..
